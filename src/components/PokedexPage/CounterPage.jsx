/*import { useState } from "react"

export const CounterPage = () => {

   const [counter, setCounter] = useState(0)
 
    const handleAdd = () => {
        setCounter(counter + 1)
    }
    const handleSubtract = () => {
     //   setCounter(state => state -1)
     counter === 0 ? 0 :  setCounter(counter - 1)
    }

  return (
    <form  className="pokedexpage__pagination">
    <button onClick={handleSubtract} className="btn__last">Back</button>
      <div>{counter * 15}</div>
    <button onClick={handleAdd} className="pokedexpage__pagination__next">Next</button>
  </form>
  )
}*/
